    # compiler = Compiler()
    # output = compiler.compile(myText.get("1.0",END))
    # T.configure(state=NORMAL)
    # if output:
    #     for i in output:
    #         T.insert(END, i)
    #     T.configure(state=DISABLED)